# Minimal deps for backend skeleton (T004+). No unnecessary packages.
# Mirrors vanguard-game/backend/requirements.txt but stripped to core + httpx for CRM adapters.
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
alembic==1.13.1
pydantic==2.5.0
pydantic-settings==2.1.0
httpx==0.27.0
# Auth (T005+): python-jose for real JWT (HS256 etc). Current uses stdlib placeholder in get_current_user.
python-jose[cryptography]==3.3.0
cryptography>=41.0.0  # for T006 token_vault envelope encryption stub (Fernet); also pulled by jose[crypto]
