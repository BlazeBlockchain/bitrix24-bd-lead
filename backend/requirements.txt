# Minimal deps for backend skeleton (T004+). No unnecessary packages.
# Mirrors vanguard-game/backend/requirements.txt but stripped to core + httpx for CRM adapters.
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
alembic==1.13.1
pydantic==2.5.0
pydantic-settings==2.1.0
httpx==0.27.0
# Auth (T005+): python-jose for real JWT (HS256 etc). google-auth for Google ID token verification.
python-jose[cryptography]==3.3.0
google-auth>=2.29.0

# LLM proxy (T007): Gemini 2.5 Flash primary + Haiku fallback. Structured JSON output.
# google-generativeai provides google.generativeai; anthropic for AsyncAnthropic fallback.
# If keys absent, llm_service falls back to deterministic mock (verifiable, zero cost).
google-generativeai>=0.4.0
anthropic>=0.28.0

# T006 vault (Fernet) + T009 full models (pgvector for profile_embedding + history.embedding)
cryptography>=41.0.0
pgvector>=0.2.0

# T026: pytest + test support
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
